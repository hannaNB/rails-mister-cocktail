<%= simple_form_for(cocktail) do |f| %>
  <% if cocktail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cocktail.errors.count, "error") %> prohibited this cocktail from being saved:</h2>

      <ul>
        <% cocktail.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.input :photo %>
    <%=  f.input :photo_cache, as: :hidden %>
  </div>
  <%# Using style to custom the upload picture file %>
  <%# <div class="form-group fileUpload btn btn-primary upload-height">
    <%= f.input :photo, input_html: { class: 'upload' }, label: 'Choose a Picture !' %>
    <%#=  f.input :photo_cache, as: :hidden %>
  <%# </div> %>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description, size: "50" %>
  </div>
  <div class="form-group">
    <%#= f.input :doses %>
  </div>
  <div class="form-group">
    <%#= f.association :ingredients %>
  </div>
  <div class="form-group">
  <%#= f.association :doses %>
  <div class="mt-3">
    <%= f.submit class: 'btn btn-primary' %>
    <%= link_to 'Cancel', cocktails_path, class: 'btn btn-danger ml-2' %>
  </div>
<% end %>

